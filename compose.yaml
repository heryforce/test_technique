services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: test_technique
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: